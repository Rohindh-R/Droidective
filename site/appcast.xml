<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0" xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" xmlns:dc="http://purl.org/dc/elements/1.1/">
  <channel>
    <title>Droidective</title>
    <link>https://droidective.com/appcast.xml</link>
    <description>Most recent Droidective updates.</description>
    <language>en</language>
    <item>
      <title>Droidective 2.6.2</title>
      <sparkle:version>170</sparkle:version>
      <sparkle:shortVersionString>2.6.2</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
      <description><![CDATA[
<style>
:root { color-scheme: light dark; }
body { font: 13px/1.55 -apple-system, BlinkMacSystemFont, sans-serif; margin: 0; }
h2 { font-size: 1.3em; margin: .2em 0 .4em; }
h3 { font-size: 1.05em; margin: 1.1em 0 .3em; }
p { margin: .5em 0; }
ul { margin: .4em 0; padding-left: 1.4em; }
li { margin: .3em 0; }
code { font-family: ui-monospace, SFMono-Regular, Menlo, monospace; font-size: .9em; background: color-mix(in srgb, currentColor 12%, transparent); padding: .1em .35em; border-radius: 4px; }
pre { background: color-mix(in srgb, currentColor 8%, transparent); padding: .6em .8em; border-radius: 6px; overflow-x: auto; }
pre code { background: none; padding: 0; }
a { color: #2f9e44; }
</style>
<p>A bug-fix release with security, correctness, and stability hardening, plus a
safer navigation flow and a clearer APK install.</p>
<h3>New features</h3>
<ul>
<li>
<strong>APK pre-install preview</strong> — opening an <code>.apk</code> from Finder (double-click, Open
With, or drag onto the icon) now stages it in the Install App screen with a card
showing the app name, package, version, target SDK, and size (read via the SDK's
<code>aapt2</code>), so you can see what you're about to install before clicking Install.
Falls back to the file name and size when build-tools aren't installed.</li>
</ul>
<h3>Improvements</h3>
<ul>
<li>
<strong>Leave confirmation</strong> — switching feature or device, or quitting, while an
active screen/mirror recording, performance/network capture, or unsaved
screenshot/video edit is in flight now asks before discarding it. Recordings
offer Stop &amp; Save; editors offer Keep or Discard. Background file pulls and
installs continue uninterrupted.</li>
</ul>
<h3>Bug fixes</h3>
<ul>
<li>
<strong>Video editor</strong> — an applied crop no longer clips away the video and its
playback controls; the crop region is shown as an overlay and still applied on
export.</li>
<li>
<strong>Screen mirroring</strong> — the video, audio, and control decoders cap how much they
buffer, so a corrupt or desynced stream can no longer grow memory toward a crash.</li>
<li>
<strong>Network speed</strong> — a <code>/proc/net/dev</code> counter that resets on reboot no longer
reports a one-off throughput spike.</li>
<li>
<strong>Reactotron</strong> — "Take snapshot" times out and reports it instead of waiting
forever when no store plugin is connected; the built-in server now listens on
loopback only (off the local network); state-tree nodes whose key contains a
slash no longer collide; and clearing one connection no longer affects another.</li>
<li>
<strong>Device info</strong> — storage reads correctly on devices with dynamic partitions,
and the battery level ignores an unrelated "Max charging level" line.</li>
<li>
<strong>File Explorer</strong> — an operation that prints a warning but still succeeds is no
longer reported as a failure.</li>
</ul>
<h3>Security</h3>
<ul>
<li>Proxy and locale values passed to the device shell are quoted, and a cancelled
action now terminates its underlying <code>adb</code> process instead of leaving it running.</li>
</ul>
<p>Installed copies update in place via Sparkle.</p>
]]></description>
      <pubDate>Sat, 27 Jun 2026 23:14:26 +0000</pubDate>
      <enclosure url="https://github.com/Droidective/Droidective/releases/download/v2.6.2/Droidective-v2.6.2.dmg" sparkle:edSignature="olGq6EDSdTbRbGk6FJKLl3xWN9FE4YZ5R2x2uYu8A8IeFWFccD7fF/FO+23ShX8vLVnICeLBBtFoFMH4dTCwDA==" length="45177627" type="application/octet-stream" />
    </item>
  </channel>
</rss>
